# 4. API Reference

This section is generated from the OpenAPI specification. The build script produces:

- **4. API Reference (OpenAPI)** — from `docs/openapi.yaml`

No markdown source is used for these pages; they are built by `scripts/build_pages.py`.
